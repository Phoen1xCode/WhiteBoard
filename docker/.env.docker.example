# Docker Compose Environment Variables
# Copy this file to .env.docker and modify as needed

# PostgreSQL Configuration
POSTGRES_USER=whiteboard
POSTGRES_PASSWORD=whiteboard123
POSTGRES_DB=whiteboard

# Server Configuration
LOG_LEVEL=info

# Better Auth
# Required: replace with the output of `openssl rand -base64 32`
BETTER_AUTH_SECRET=replace-with-a-32-plus-character-high-entropy-secret
BETTER_AUTH_URL=http://localhost:3000

# Frontend Configuration
# Set these to your production domain
VITE_API_BASE=http://localhost:3000
VITE_WS_URL=http://localhost:3000

# For production with a domain:
# VITE_API_BASE=https://api.yourdomain.com
# VITE_WS_URL=https://api.yourdomain.com
